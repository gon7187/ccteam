# ccteam 当前状态(`.loop/state.md`)

> **本文件的家**:当前焦点 · 基线数字 · 人工门登记 · 未固化教训 · 流程速查。
> 维护者 = 规划(控制)会话,每版 ship / 每波收口时回填;**dev 会话只读**。
> 瘦身纪律:落盘前自问「下个 session 第一屏需要它吗?」——完成细节沉 `git log` 与
> `docs-local/versions/`(gitignored),教训固化进 `.loop/verify/README.md` / AGENTS.md 后此处只留指针。

## 当前焦点

- **版本线**:workspace **`0.10.0` 已发布**(2026-08-16,owner 把 v0.9.15 周期改名 v0.10.0 一并发版:「v0.9.15 改为v0.10.0,pr改名。然后tag和release」→「pr 改完版本号之后你直接合并。然后发版」)。**内容 = 两块合一版**:① DSH 第七 vendor「插件即集成」(2026-08-15 owner 直驱,规划派 5 只 codex 子会话四波并行开发,W0 真机双模式 DoD 全 PASS)② DSH web 一等公民 —— ccteam web 新增顶级「DSH」菜单(伴生端口反代 + 每身份隔离空间,SPA 由规划会话亲自设计;owner 2026-08-16「ui 你 fable 亲自设计,codex 审美不行」)+ 上线后真机 dogfood 抓出并修复三个生产 bug(`start_for` 自死锁 / 租户 patch insert 重复+嵌套 config / `crypto.randomUUID` 安全上下文缺失)。**执行链**:ship gate(Cargo.toml 0.10.0 bump + AGENTS.md/state.md/history.md/backlog/tech-design §10 回填,`a5cdc8bc`)→ PR #180 改名「v0.10.0 cycle」→ **CI 首跑一红一绿两次独立复现同一族 gateway echo 测试双竞态**(`turn_answer_carries_context_echo_for_focused_im_session` 与 `turn_answer_context_echo_omits_role_when_roleless`,从未同时红;根因 = `FakeAdapter::events()` 跨身份共享一个 `Notify` 的纯测试替身竞态,`gateway.rs` 零改动,登记 `.loop/verify/README.md` env-flake 族,详见该文件)→ 第三次独立重跑三 job 全绿(run 31916013891)→ **merge commit `76860415`**(非 squash,`gh pr merge --merge`,dev 即成 main 祖先)→ main CI 绿(run 31916139457)→ annotated tag `v0.10.0`(`d1f2d51`)→ release run **31916252550** success(四平台 build-jobs + publish-release 全绿),`/releases/latest` 已指向。**产物实测**:下载 linux-x64 → `sha256sum -c` OK → 跑出 `ccteam 0.10.0 (7686041)`(build sha 对齐 merge commit)+ static-pie stripped ELF。上一发布 = v0.9.14(2026-08-12,main `fb2a9540`)。**gh 按机器分**:macOS 机 = `/opt/homebrew/bin/gh`(firstintent,repo+workflow scope);**Linux dev 机(ccd)= `~/.local/bin/gh`**(firstintent,scope 仅 `gist,read:org,repo` —— **无 `workflow`**,改 `.github/workflows/*` 须走 SSH push;git protocol 本就 ssh,普通推送不受影响。本次实锤:`gh pr edit` 因无关的 GraphQL Projects-classic 弃用警告整体报错、标题实际未改,改用 `gh api repos/.../pulls/180 -X PATCH` 绕过)。
- **RESTART-1 已落 dev(2026-08-19,`905e4a9`,owner 直驱「从 ccteam 代码本身解决,需要一个完善的方案,之后执行」)**:daemon 重启后同 sid 双进程(本机 s989 双写实锤)的病根 = 停机「留进程、说会 reattach」与启动恢复「当全死、逐 sid 再 spawn」两层假设矛盾;修法 = 一条合同「一 sid 一 body」——harness `session_body` 记录/探活(pid+指纹+environ)、gateway `plan_session_rebuild` 单一闸门(活体 → `detached` 登记 + 排队 + 显式 stop,死记录 → post-mortem)、body watcher 退出后从 vendor transcript 恢复未观测回答 → 回投/委派通知 → 按 sid 重建 → drain;停机改确定性 detach(不 kill)。基线 1976/0(+12);真机 drill 全链路 PASS(详 backlog 卡「验证」段 + `docs-local/bugs/2026-08-19-daemon-restart-duplicate-session-bodies.md`)。**人工门候选**:是否把「stream-json 真 reattach = session-host 托管进程」立 PRD(本卡为其留门);codex/ACP 重启仍打断 in-flight turn(现状)。
- **2026-08-25 TG 三线(owner 直驱,规划 = Fable 5 s45 编排 codex luna 开发 / opus + gpt-5.6-sol max 交替 review)**:TG-STOP-1 + TG-FMT-1 + RU-I18N-2 + clippy 1.98 修复合入 dev `012f0ab3`(PR #188 周期),细节见 backlog 卡「验证」段。**基线**:本机 `make test-baseline` = **2013 passed**(上口径 1976)。⚠️ **origin/dev 既有确定性红(非本波所致,pristine origin/dev 复现)**:`gateway::tests::delegation_{all_and_off_modes,e2e_spawn_dispatch…,final_mode…,reconcile_delivers…,reconcile_folds…,watch_is_spent…}` + `mcp::dispatch::session_tool_tests::{dispatch_wait_inline…,dispatch_wait_skips…,spawn_with_task_and_wait…}`(9 只,隔离亦红)+ `turn_answer_{carries_context_echo_for_focused_im_session,context_echo_omits_role_when_roleless}`(断言 `→ alpha/s1` 尾串,疑随俄语本地化提交失配)—— 需立卡修,勿再当 env-flake 登记。环境教训:本机 `/tmp` 会在不重启的情况下被清空(worktree/日志/子会话一并没),worktree 改放 `/root/projects/ccteam-wt/`。
- **下一版**:owner 点名;候选 = A2A 可观测性补丁(A2A-OBS-1/3/4/5,A2A-OBS-2 先待重划)或 TD-SYNC-1 全文校对;v0.9.4(npm 分发)gated 不变;Loop Engineering PRD(`docs-local/versions/v0-x-loop/`)草稿标题写的 v0.10.0 已被本版占用,拍板时需换号。**周期纪律**:下个周期首个新提交即开 dev→main draft PR(AGENTS §五)。

## 基线(口径与 env-flake 族见 `.loop/verify/README.md`;只增不减)

- 确定性口径 `make test-baseline`(`--lib --bins`)= **1895/0**(v0.10.0,Linux dev 机(ccd)本会话实测,7 个 target;上口径 1864(v0.9.14 已发布)+31 = DSH 第七 vendor(enum slam 穷举臂测 + `dsh_acp_test` fake ACP 对端 9 + `materialize` 单元测 9 + IM/web 发现面测)+ DSH web 一等公民(`dsh_web_test` 3,含死锁回归 + `materialize` 结构断言追加);插件自身测(`plugins/dsh-client`,node/vitest,不计入本 Rust 口径)另计 10/0。**干净环境仲裁 = PR #180 CI 三 job 绿**。macOS 宿主两族(TMPDIR 形状 / UDS SUN_LEN)登记不变,修卡 TEST-MACOS-1,Linux 不受影响
- `make test`(workspace 除 web,**全 target**)= **124 bins / 2638 通过 / 0 失败**,exit 0(v0.9.14 收口本机实测;上口径 123 bins / 2564)。⚠️ **计数纪律**:`make test 2>&1 | grep …` 后台跑会因 grep 块缓冲**丢尾段**(实锤:同一次跑先读到 60 bins/1383,重跑落盘完整文件是 120 bins/2506)—— 记基线数字一律**先落盘完整日志再 awk 汇总**,不要在管道里边跑边 grep
- `ccteam-web` 全量:**379 通过 + 3 红**(`cargo test -p ccteam-web --no-fail-fast`,36 bins;上口径 351+3)= `pty_ws_test` 的 `ws_receives_pane_bytes_from_pipe_pane` / `ws_two_clients_share_one_pipe_pane` / `ws_last_client_disconnect_stops_pipe_pane`,已登记 env-flake 族(本机确有 live daemon 在跑,非回归)。**注**:`make test-web` 配方**没有** `--no-fail-fast`,`ws_*` 一红即中止余下 binary —— 记全量数字请自己加 `--no-fail-fast` · vitest **612**(55 文件;上口径 606/54)· tsc(`npx tsc -b`)/ eslint 干净 · Playwright **7**(未重跑,沿用口径)
- clippy **0 warnings**(`-D warnings`,含 ccteam-web)· `cargo fmt --all -- --check` 干净

## 人工门(不许任何 agent 在任务内自决;签核 = 一次性授权,登记于此)

| 事项 | 状态 |
|---|---|
| **v0.10.3「DSH 会话一元化」拍板 + 开发开工(门① operator 真 `~/.dsh` 插件注册豁免 + 门② 退役 per-hire 子进程/per-sid `DSH_HOME`)** | **已签核,开发完成(dev 已推,bump/merge/tag 另等 owner)** —— owner 2026-08-18 启动开发令(「启动 v0.10.3 开发…本条消息即『启动开发』令,同时视为 §8 两道门的签核」+ 追令「subagent 用 opus xhigh」):方案 SoT = `docs-local/versions/v0-10-3/PRD.md`。**门①** = 幂等注册 `@ccteam/dsh-client` 进身份 profile **含 operator 真 `~/.dsh`**,计入 vendor 足迹红线「自家注册」豁免(merge-only、只碰自己的段);**门②** = 退役 per-hire 子进程与 per-sid `DSH_HOME`,历史 DSH 会话 vendor 侧 resume 不迁移直接退役(pre-v1.0)。**执行结果**:P0 spike 四问全 PASS(真机,`spike-notes.md`;附带发现 turn 归属过滤义务/sun_path 108B/reuse-live-first 三条契约增补)→ 拆卡 DSH1-A/B/C/P4 → 3 只 opus subagent 交付(A 插件 socket 传输 v2 `3f419350` / B `DshRuntimeManager` 下沉 `3d7f77ab` / C connect 代替 spawn `7919c4f7`,C 顺手修 cancel-notification 被 writer abort 吞帧真 bug)→ 规划亲收 P4 `ea612c0b`(Hosts 一键注册 + docs 五处 + tech-design + **真机 DoD ①②③ PASS**,④ 方式二回归测试覆盖、live 留 owner dogfood;真机抓出两个跨单测防线 bug 并修:缺席 profile 脚手架 / Cordis inject 守卫,`dod-notes.md`)。门禁终值:baseline **1961/2**(两红均在册族,1947→+14)、clippy 0、web-check 689/689、插件 vitest 24/24。**余项二**:①周期 draft PR **未开成** —— PR #185(v0.10.2)已被 owner 于 2026-08-18 10:25 合并(main=`942abc33`),而本机 gh 已易主:`~/.local/bin/gh`(firstintent)不存在,`/usr/bin/gh` = `zerohacker666` 非 collaborator(`gh pr create`/API 均拒),SSH push 正常;**开 v0.10.3 draft PR 需 owner 或 macOS 机 gh 执行**,开启前推 dev 零 CI 覆盖(verify/README 已载),开启后回填 CI 仲裁值;②方式二 live 重跑留 owner dogfood(单测 24/24 已覆盖) |
| **v0.10.1「一个身份一个 DSH 配置家」拍板 + 开发开工(D26 取代 D22)** | **已签核,执行中** —— owner 2026-08-16:PRD(`docs-local/versions/v0-10-1/prd.md`)「按推荐确认,你派 codex 去开发,提交 dev」= **D26**(租户 DSH web 家默认 turnkey 种子自 operator 二件套 + 撤租户面 `DEEPSEEK_*` env 剥除;**显式取代 v0.10.0 D22 的出厂空白默认**)· **D27**(「不种子」开关本版不做——只关 web 面是安全剧场,真开关须两面同门另立版本)· **D28**(refresh-if-unmodified:sha 标记,未动跟随源刷新、动过永不碾压)。授权范围 = 开发 + 提交推 dev + 开周期 draft PR;**bump/merge/tag/release 另等 owner 发话**。**执行结果**:codex s302 四段落地(`f02938a0` 解析器+owner 穿线 / `68c4bbd5` 种子+跟随+de-scrub / `15c0398a` docs / `3e0574d4` 卡写回);规划 review 三承重点全核(解析器两面共用单函数 · 二件套同源由构造保证 · 无标记旧家判「已修改」保护);门禁 `make test-baseline` **1895→1896/0** · clippy 0 · fmt 干净 · writeback 绿;draft **PR #182** 追踪周期。**真机升级路径实证**(本机部署 `3e0574d4` 后):租户 rob 的 pre-v0.10.1 空配置家在实例重启时自动种子(credentials/settings byte==operator 源 + `.ccteam-dsh-seed.json` 标记),既有 mychat 工作区原样保留,`host.describe` 从空配置反转为 `provider: aliyun | model: deepseek-v4-pro` 且 attachedSessions=1(用户已实际连上)——触发本版的原始报告场景已闭环 |
| **v0.9.15 周期改名 v0.10.0 + PR 改名 + 直接合并 + tag + release** | **已签核并执行完成** —— owner 2026-08-16 三令:①「v0.9.15 改为v0.10.0,pr改名。然后tag和release」②「pr 改完版本号之后你直接合并。然后发版」= 显式授权本会话执行 merge(非仅「改名待 owner 点击合并」)。**执行结果**(详「版本线」行):PR #180 改名「v0.10.0 cycle」→ CI 两次独立触发同一族测试替身竞态(登记 verify/README,非回归,`gateway.rs` 零改动)→ 第三跑三 job 全绿 → `gh pr merge --merge` 落 `76860415` → main CI 绿 → tag `v0.10.0`(`d1f2d51`)→ release run 31916252550 success,四平台产物实测跑出 `ccteam 0.10.0 (7686041)`。**Loop Engineering PRD 草稿(`docs-local/versions/v0-x-loop/prd.md`)标题写的 v0.10.0 因此被占用**,已在「下一版」行留痕提醒,拍板时需换号 |
| **v0.9.15 追加特性「DSH web 一等公民」方案拍板 + 开发开工(W1 探针 → 拆卡 → 派 codex 开发 → 推 dev)** | **已签核,执行中** —— owner 2026-08-16 五令:①「ccteam web 新加 DSH 菜单,打开 = dsh web 同样内容,适配多租户」②「把 dsh web 提升为 ccteam web 一等公民」③「用 DSH_HOME 隔离,每租户独立 dsh 配置,每用户打造自己的 dsh 插件体系」④「operator DSH 页加『打开原生 DSH web』按钮…其他你按照推荐走」= D20–D25 全按推荐拍板(伴生端口反代 / attach-if-detected / 租户凭据自助 / v1 特权全放 / v1 无自动回收 / 版本归属 v0.9.15)⑤「之后启动开发,你派 codex 来开发。最终提交 dev 推送」⑥「ui 你 fable 亲自设计,codex 审美不行」= **DSHWEB-SPA(`crates/ccteam-web/web`)由规划会话亲自实现**(owner 点名例外,代码面写权本卡内有效;s258 已改派工摘除该域)。方案 SoT = `docs-local/versions/v0-9-15/prd-dsh-web-menu.md`(事实基础 = 双侦察 + codex s255 真机探针 + grok s256 上游调研,均 2026-08-16);卡 = DSHWEB-W1/CORE/TENANT/SPA/DOCS。授权范围 = W1 真机探针 + 代码开发 + 提交推 dev(PR #180 周期内);**merge / tag / 发布仍归 owner**。W1 证伪代理可行性(#1)须停手回报。**执行结果**:W1 七项全 PASS(代理可行);s258 codex 落后端(CORE `97413d6` supervisor+伴生监听+反代+REST / TENANT 同 commit 合并式物化+enrollment 注入+凭据自助 / DOCS `6fb6726`)+ 规划亲自落 SPA(`b3bd1e37` DSH 菜单+DshView+原生按钮);规划收口修一处 clippy(`io_other_error`)。门禁:`make test-baseline` **1887→1895/0**、clippy 0、fmt 干净、vitest **622/622**、`dsh_web_test` 2/2、vite build 出 dist。`dsh_acp_test` 4 红 = `tests/*.rs` env-flake(需 default-model 源,隔离壳缺;origin/dev 基线同红,不计入 baseline 与 CI,见 verify/README §env)。推 dev 进 PR #180;merge/tag/发布仍归 owner |
| **v0.9.15 开发开工(派 codex 子会话 + 推 dev + 开版本 PR + 真机 W0)** | **已签核并执行完成** —— owner 2026-08-15:「你作为规划者把控架构,你来派 ccteam codex 子会话开发这个需求,尽量大粒度任务,允许把没有冲突的任务用并发多子会话执行,但注意不要硬拆并行。节约主会话 claude 上下文占用。最终推上 dev,开版本 pr。真机用本机已经装了 dsh 可用。要在一个版本里完整接线上线,不要遗留。其他未决定来按照你的推荐走。」授权范围 = 开发 + 推 dev + 开 dev→main PR + W0 真机。**执行结果**(详「在做」行):四波 5 子会话全部完成,`make test-baseline` 1864→**1887/0**,clippy 0,fmt 干净,真机双模式 DoD 全 PASS,PR #180 已开(draft)。**merge 仍归 owner,tag / 部署 / `npm publish`(D18)仍等 owner 显式发话**;未决项(D7/D12/D15/D16/D17)按规划推荐消耗,新增 D19 已闭环 |
| **v0.9.15(DSH 第七 vendor)需求定稿 + 架构方向钦点** | **已签核(需求面;开发未授权)** —— owner 2026-08-15:「review 这个版本需求,然后把需求定稿。这是一个和其他 vendor 都不一样的,需要规划一个符合 DeepSeek Harness 理念的架构上 ccteam 集成的方案。DeepSeek Harness: Everything is a Plugin.」规划会话据此把 `docs-local/versions/v0-9-15/` 从 hybrid B 草案改写为**案 P(插件即集成)**定稿:一个 Cordis 插件双面孔(8 工具面 + 受管传输面 serve ACP,含 `session/load` 冷 resume 真做/富事件/审批),受管 runner = 产品 `dsh --profile ccteam` + 隔离 `DSH_HOME`,`execution/dsh_acp/` 坐共享 `acp/`,LocalOnly、零 writer、beacon 与 8 工具不变;原「冷 resume 豁免」撤销。门表处置 = 该目录 `decisions.md`(D1 版本号 0.9.15 已批;余门 D7 W0 执行者 / D12 排期 / D15 价表 / D17 方式二 UX 边界 / D18 插件 npm 发布)。**同日 owner 追加钦点双形态**:「方式一 ccteam 集成 dsh(ccteam web 入口)· 方式二 ccteam 作为 dsh 插件(dsh web 入口,插件生态)。这两种方式打算都考虑上」—— 方式二由附属路径升格为一等产品形态(PRD §〇/§2/§3.7:接入引导 / npm 发布 / 完成通知注入 SHOULD / UI 边界),架构零变更(同一插件双面孔本就承载两形态)。**授权边界:仅需求定稿;排卡、W0 真机、写代码均未授权** —— W0 探针(第一承重 = 跨进程 resume)阻塞 adapter/mount,W0 证伪传输面须回落 hybrid B 并先报 owner |
| **v0.9.14 发布(bump + ship gate 回填 + 开 dev→main PR + merge + tag + release.yml)** | **已签核消耗**(v0.9.14 已发布 2026-08-12)—— owner 2026-08-12「把 dev 分支上的内容,都 release 流程」;版本号 = 规划按 0.9.x 节奏定 `0.9.14`(owner 未点名字面,循 v0.9.11/12/13 先例的补丁位累加;v0.10.0 仍留给 Loop Engineering PRD)。**含一次性治理写权**(本会话据此写 AGENTS.md §一/§三/§四 + `.loop/{state,history}.md` + `docs/`):其中 **§三红线两处为「表述随已落地代码同步」而非新增/删除红线** —— ①「session 调度门」行的「主会话 fallback = HTTP admin bearer / stdio forwarder 读 web-token」整段已被代码删除,改写为 enrolled client(`initialize` 签发身份 + 不进 live map + 必须点名 workspace + 进程血缘补位);②§四 MCP 行的 stdio `mcp-serve` 表述同理。语义边界:红线本身(**授权只认 principal、任何自报属性不参与**、fail-closed、不推断项目)一字未松,反而被收紧(admin 层从数据面整体消失)。owner 如认为该表述变更需单独签核,请在此行批注。**常态不变**:治理面写权归 Fable 5 规划会话,下次仍须停手申报;push main ≠ 发布,下个版本 tag 仍需 owner 显式发话 |
| **README/usage 增补「先装并登录至少一个 vendor CLI」+ tag 后的 docs 修补合入 main** | **已签核消耗** —— owner 2026-08-12 IM(附 T3 Code 的 Installation warning 截图)「readme.md 增加类似这个图片的内容,让用户预先安装 Claude、Codex、Grok、Pi、OpenCode,至少一个」;落地 = README Install 段一张 `[!IMPORTANT]` 卡(六 vendor:安装链接 + 登录命令,均按**本机实测**的 CLI 子命令写:`claude auth login`/`codex login`/`grok login`/`opencode auth login`/`kimi login`/pi = provider key + `pi auth check`)+ `docs/usage{,-cn}.md` 同表。顺手修 CN 手册残留的 systemd/launchd 段(v0.9.7 已退役,EN 版早已正确)。**tag 后追加**:owner「Important 中的 pi 换成这个链接 https://pi.dev/」→ `1364bdb7`;owner「让 dev 和 main 保持一致」+「直接合并 pr」= 对 **PR #178** 的显式合并授权(docs-only,不影响已 tag 的产物),落 `92fe0c3f`。**先例**:tag 之后的纯文档修补可再走一次 dev→main PR 让 README 前台页与 dev 一致,但**不重发版本**|
| **v0.9.13 发布(bump + ship gate 回填 + 开 dev→main PR + merge + tag + release.yml)** | **已签核消耗** —— owner 2026-08-08「拉最新dev,然后走release流程。正式发布」;版本号 = 规划按 0.9.x 节奏定 `0.9.13`(owner 未点名字面,循 v0.9.11/v0.9.12 先例的补丁位累加;另:PR #175 标题已用「v0.9.13 cycle」,与本次一致)。执行链见「版本线」行。**含一次性治理写权**(本会话据此写 AGENTS.md §一/§〇/§四 + `.loop/{state,history,backlog}.md` + `docs/`):常态不变,治理面写权归 Fable 5 规划会话,下次仍须停手申报。**常态不变:push main ≠ 发布,下个版本 tag 仍需 owner 显式发话** |
| **Pi 的 `--system-prompt` 是否触碰「No prompt injection」红线** | **待 owner 裁决(不阻塞本次发布)** —— Pi 无 vendor 原生 `--agent` 读取面,故 `pi_rpc/role.rs` 把用户 `.claude/agents/<role>.md` 的 **body 原文**(零 ccteam 措辞、sha256 记账)投射成文件并经 `--system-prompt` 传入。与红线字面(禁 `--append-system-prompt` / `initialize.systemPrompt`,ccteam 不注入**自己的** system prompt)不冲突,但与「agent 行为一律由 **vendor 自读自己的文件**得来」的表述有张力:这里是 ccteam **指给** vendor 读,而非 vendor 自己找。两条出路(owner 选):① 判定「内容 100% 用户的 + 零 ccteam 追加」即合规,红线行补一句把「ccteam 代为指路」写清;② 判定不合规,则 roleless-only(同 grok/opencode/kimi 现状)。**在裁决前不扩大**:除 role body 外,任何 ccteam 自撰文本一律不得进这条通道 |
| **§三红线增删:HITL 行删除 + role 概念整体退出红线** | **已签核消耗** —— owner 2026-08-03「ccteam 的 HITL 与 role 红线…把这两点从 ccteam 的红线中删除」+ 追问确认「role 这个概念整体退出红线」。落地:§三删「HITL 批准边界」整行 · 「session = 独立一等实体」行去 role 副标题(「同一 role 可并存多 session」→「sid 是唯一身份,任何属性都不参与去重」,反 dedup 守卫的家移至 §〇 退役概念行)· 「No prompt injection」行去 role 表述(留「不注入 system prompt + vendor 自读自己的文件」,并把 `--append-system-prompt`/`initialize.systemPrompt` 的禁令写进红线)· 「init 布局」去标题与正文的「不种任何 role」· 调度门行「role 只是审计标签、不参与授权」→「授权只认 principal,任何自报属性都不参与」· 挤停行与跨机行的 HITL 措辞泛化为「等待用户决定」(安全性质不变)· 第 14 行删「ccteam 唯一管的指令面 = `.claude/agents/<role>.md`」· 内容红线「role/agent/…」→「agent/persona/…」;§四 role 库行去红线口吻;`docs/dev/tech-design.md` §0 R3 gloss + §6.2(顺手修「init 种默认 cto.md」的 v0.9.0 陈旧句)+ ccteam-flow 行同步。**语义边界**:role 与 HITL **仍是产品特性**(`--agent` / `/role` / vendor 原生批准门的代码、REST/MCP 契约、`docs/usage.md` 全部不动),只是不再是架构红线、不再是任何红线的守法前提。**含一次性治理写权**(owner 直驱本会话写 AGENTS.md + `docs/` + 本文件);**常态不变**:治理面写权归 Fable 5 规划会话,下次仍须停手申报 |
| **v0.9.12 发布(bump + ship gate 回填 + 开 dev→main PR + merge + tag + release.yml)** | **已签核消耗** —— owner 2026-08-02「把最新的积累在 dev 分支上的发 release」;版本号 = 规划按 0.9.x 节奏定 `0.9.12`(owner 未点名字面,循 v0.9.11 先例的补丁位累加)。执行链见「版本线」行。**含一次性治理写权**(dev 会话据此写 AGENTS.md §一 + `.loop/{state,history,backlog}.md` + `docs/`):常态不变,治理面写权归 Fable 5 规划会话,下次仍须停手申报。**常态不变:push main ≠ 发布,下个版本 tag 仍需 owner 显式发话** |
| **本机 dev daemon 换装重启(部署 `410647d5` binary = tenant `/status` ACL 修复上线)** | **已签核消耗** —— owner 2026-07-31 IM「拉最新代码…彻底修复」(rob 租户 `/status` 无 👥 直接子会话,web 拓扑可见):排查实锤病根 = **修复从未上线**,代码本身正确(见教训行);行动 = 410647d5 实体安装至 `~/.local/bin/ccteam` 与 `/data/.local/cargo/bin/ccteam`(替换断链软链与 Jul-2 旧拷贝)+ 脱离式脚本重启 daemon(旧映像备份 `/tmp/ccteam-old-running-backup` 可回滚,脚本+日志 = `~/.ccteam/redeploy-20260731.{sh,log}`)。范围仅本机 dev daemon;tag/发布常态门不变 |
| **v0.9.11 发布(merge PR #171 + tag + release.yml)** | **已消耗(v0.9.11 已发布 2026-07-29)** —— owner「合并pr,tag 和发release」;执行链:PR CI 三 job 绿(head `a01150e3`)→ ready → **merge commit** `efce0196`(非 squash,dev 已成 main 祖先、免和解可直接续用)→ main CI 绿 → annotated tag `v0.9.11` → release run 30427416169。常态不变:下个版本 tag 仍需 owner 显式发话 |
| **v0.9.11 团队页重设计开工(PRD 全默认拍板 + 版本号钦点 + 推 dev)** | **已签核消耗** —— owner 2026-07-29「确定,启动开发。提交推送到dev即可。pr已有 #171」= PRD `docs-local/versions/v0-9-11/prd-team-page.md` 待拍板五项全默认(v0.9.11 / roster+timeline 直删 / 全局 routing web 只读 / 起手卡文案入 repo / P2 面不做);授权范围 = 开发 + 推 dev(发布另见上行);同日 owner 追加「后选动工,派 subagent opus」= TEAM-7..10 三候选 + 收尾卡授权 |
| **v0.9.10 发布(merge PR #170 + tag + release.yml)** | **已消耗(v0.9.10 已发布 2026-07-26)** —— owner 深夜授权「验收通过后…直接tag + release,明早 github release 见二进制」;执行链全绿:merge `180e91b` → tag `v0.9.10` → release run 30218361955 success(四平台 tarball + SHA256SUMS,`/releases/latest` 已指向);常态不变:下个版本 tag 仍需 owner 显式发话 |
| **tag + 部署** | **已消耗(v0.9.8 已发布)** —— owner 2026-07-23「人肉测过了,打tag、发release」→ 正式 `v0.9.8` tag(main squash `ad1c7c2`)推送,release.yml 全绿(四平台 tarball + SHA256SUMS);`/releases/latest` → v0.9.8,全体用户经 `install.sh`/`ccteam update` 可拿到(v0.9.8 无 rc,owner 已先行人肉测)。上一次 = v0.9.7(`2922f7a`,rc 先行)。**常态不变:push main ≠ 发布,下个版本 tag 仍需 owner 显式发话** |
| V097(v0.9.7 daemon 重构 + update)W0 拍板 | **已签核消耗** —— owner 2026-07-22「install.sh 检测 systemctl…你来调度进入开发,提交 dev,发 PR」;废 systemd/launchd 先期拍板 + D1–D8 按 PRD v4 默认全「是」消耗(**含 D2 `daemon stop --force` SIGKILL 例外,仅 daemon 自身,agent session 零碰**);merge PR #165 = owner 2026-07-22「已经合并」;`825ae7d` squash 落 main |
| v0.9.6 compare 契约删除(REST `/compare`×2 + IM `/compare` + web tab) | **已签核消耗** —— owner 2026-07-21 会话拍板「compare 去掉,改会话内编排」,落 dev(T4) |
| v0.9.6 docs 写权一次性授权(kimi 改 usage/orchestration/tech-design/README) | **已签核消耗** —— owner 2026-07-21 指定 kimi 更新全局文档、fable5 review;仅本版有效,写权常态仍归规划会话 |
| v0.9.6 合 main | **已签核消耗** —— owner 2026-07-21「review 后合并 main,让 dev 和 main 保持一致」;fable5 review 三提交(3e6bca1/9c5f895/86b9788)后 ff 合并 |
| AGENTS §三 init 布局红线行澄清(注明用户可选 `.ccteam/routing.md`,init 不种) | **已消耗** —— 随 owner ship 9c5f895 的语义校准,非新增红线 |
| v0.9.4 动代码 | gated —— owner 暂缓(2026-07-17,v0.9.5 先行;v0.9.5 已于同日完成落 main,授权已消耗) |
| 分支治理 = dev + PR 攒版本(常态规则,非一次性) | **已生效** —— owner 2026-07-22:「后续新功能开发一律在 dev 分支开发,提交 PR;多个提交累计组成一个版本,owner 合并 PR 后复用 dev 重复」;取代旧 direct-on-main。**2026-07-24 owner 补充拍板:合并方式 = merge commit(非 squash)**(main 含完整历史、免每轮和解——v0.9.8/v0.9.9 两轮 squash 和解成本实证)+ 周期开始即开 draft PR(dev 推送借 PR 跑 CI);均已固化 AGENTS.md §五「分支与推送」 |
| 外部 Agent MCP 接入 Phase 1(研究稿 `docs-local/research/external-agent-mcp-symmetric-architecture.md` 待拍板 D1–D10) | **已签核消耗** —— owner 2026-07-23「实现这个需求」= 按稿内推荐默认拍板,授权范围仅 Phase 1(主 daemon WebUser MCP,tenant token 直用为 MVP);Phase 2(独立 MCP token)/3(卫星 relay)/4(多 Authority)未授权;8-tool wire schema 不变 |
| v0.9.9 需求决策委托 + 开工(全局 skill 库 PRD FREEZE 解锁) | **已签核消耗** —— owner 2026-07-24「review v0-9-9 版本需求→不恰当处由规划决策改良→完成开发→治理沉淀清理→提 PR,owner 合并」;规划决议落 `docs-local/versions/v0-9-9/decisions.md`(O1–O5 钉死 + ADJ-1 全局面 admin-only + ADJ-2 rm 防误删 + ADJ-3 并入复盘 P0-1 wait 240,**wire schema 不变、additive 字段**;复盘其余排 A2A-OBS 卡)。merge 权仍在 owner |
| AGENTS §三红线**表述**治理(GOV-CE-2:瘦身 + resume-by-sid 行并入 session 一等实体行,语义零变更) | **已签核消耗** —— owner 2026-07-26「批准」chat 提案(逐行判定 + 重写样例);落地 `f0c834c`。红线**增删**类变更 = **A/B/C/E 已签核消耗**(owner 2026-07-26「批A B C」+ 追加「E删除」,落地 `23d0cef`:新增 引擎零 LLM / daemon 无自主内容决策循环 / vendor 配置足迹唯一写,README 行迁家 §五.7);**D(never-execute 并入 #15)未批不动** |
| MCP `screenshot` 工具删除(wire 契约变更,8→7)+ 关联面全删扩令 | **已签核消耗** —— owner 2026-07-26 IM「删除 mcp__ccteam__screenshot,这个是 tmux 时代的遗留产物」= MCP 工具面(1ab85da);同日追加「screenshot 关联的一并删」= web `/screenshot` 路由 + core 渲染管线与四依赖 + IM `/screen` 全退役(614ed9b);§三「不解析终端输出」行措辞随令两次同步(语义不变,pane-snapshot 仍为只读路径);chat_send_file 同问评估 = 保留(IM 外发通道,报告 owner 无异议) |
| MCP 裸名发现别名(wire 契约变更,7→8) | **已签核消耗** —— owner 2026-07-26「改吧…opencode排到最后」→ 中途改令「纯别名把」「方便后续随时改」= 新增 `claude_codex_grok_kimi_opencode_status`(status 纯 alias,ffcf817);名字自 AgentVendor::ALL 派生测试锁死,后续改/删只动别名不动 status |
| MCP `session_spawn` `protocol` 参数删除(wire 契约变更:schema property 删除 + 传入即硬错) | **已签核消耗** —— owner 2026-07-26 IM「在 mcp 调用的时候删掉协议字段…每个 vendor 只选一种最佳的协议…这个字段用默认,没必要让调用方去传」;每 vendor 恰一通道(claude/codex=stream-json,grok/opencode/kimi=acp),先例对齐 host 参数(`HOST_SPAWN_PARAM_REMOVED`);落地 = MCP-CULL-3(codex 委派);REST/web 协议面(terminal/rmux admin beta)不在本令,退役另案 |
| MCP beacon 别名改名(wire 契约变更:`claude_codex_grok_kimi_opencode_status` → `grok_claude_codex_kimi`,旧名即删) | **已签核消耗** —— owner 2026-07-26 IM「必须优化 a」钦点新字面(grok 打头、去 `_status`、opencode 出列);仍为 status 纯别名(BEACON-1 决议「纯别名方便随时改」正是为此);落地 = MCP-DX-3(codex 委派) |
| spawn project 默认梯扩展(解析语义变更 + `config.yaml` 新键 `default_project` + `~/.ccteam/default_project` scratch 自动供给 + 响应 `project_source` 字段) | **已签核消耗** —— owner 2026-07-26 IM「必须优化 b」拍板服务端默认项目方向,「还有更好的方式吗」= 授权规划定形(五级梯 + configured 键 + lazy scratch);tenant 语义零变;落地 = MCP-DX-3 |
| status 返回瘦身(MCP status JSON 形变 + core `ProjectState` 退役字段 team/phase/tmux_session 全链清除) | **已签核消耗** —— owner 2026-07-26 IM「status 返回冗余…从架构多层通用性和对称性去修」+「省 token…不能变 token 刺客」+「其他省 token 也你来决定,功能优先其次省 token」;落地 = STATUS-SLIM-1(codex 委派,排 MCP-DX-3 后) |
| web ACL 收敛(§三 ACL 行「全局·运维面仅 admin」修订 + REST 契约 403→200 放开(status/hosts/skills/项目 MCP 注册/global-attach)+ tenant 自助 reset-token) | **已签核消耗** —— owner 2026-07-27「除了新增用户,其他的功能应该放开给普通用户…在web进行调整。权限的底层逻辑应该是用户token和绑定的项目」;落地 = WEB-ACL-1(`1025450`),红线行文随治理收口 commit 同步。边界判定(规划,owner 如要更宽另令):全局 bot 凭据 `/config/im` 保持 admin(身份对称 —— tenant 家 = `/me/im`,各人管各人凭据);terminal 协议 UI 门保持(冻结面非本令);登录链接卡 admin(= 他人 token 面) |
| 治病根优先于补丁(常态规则,非一次性) | **已生效** —— owner 2026-07-28:「从架构层的通用性来设计解决真正的病根,而不是补丁式的。把这条加入 agents.md」;已固化 AGENTS.md §五 总纲(两条判据:同形扫一遍 / 新入口自动被覆盖;并覆盖测试定性——「登记为 flake」常是病根未找到)。同日串用户扫荡为实锤来源 |
| ACP 结局契约修复开工 + 推 dev(owner 直驱,无卡) | **已签核消耗** —— owner 2026-07-30 IM「分析本机这个kimi会话为什么卡主,从ccteam架构层面分析。修复。不要打补丁而是分析kimi的适配和架构层面修复」+「有更新直接提交dev分支推送…如果没问题不要硬修,从架构通用性、对称性的用户体验考虑优化」+「推送」;落地 `410647d`(rebase 于 `3be807b`)。授权范围 = 代码面修复 + 推 dev;**不含** tag/发布/开 PR。owner 「不要硬修」被当作判据用于两处**主动不修**:kimi `failed→end_turn` 折叠(需解析 vendor 私有 log 布局)与 kimi `ctx —`(vendor 不上报,编数=造数据) |
| **开发会话日耗上限 15 USD / 自然日 + 自主连跑(常态规则,非一次性)** | **已生效** —— owner 2026-08-01:「每天日耗控制在15U以内。可以一直跑。不用问我。写入治理」;已固化 AGENTS.md §五.9。语义:预算内**不请示**、持续取活;逼近上限**减小规模**(缩 wave/少派 subagent/降档)而**不停工**;省额度不换质量(测试门与基线红线不动)。与产品面 `budgets.*.max_cost_usd_per_24h` 无关(那是 agent session 的自动停,本条约束开发会话自身花法) |
| 一次性治理写权授予 dev 会话(2026-08-01 MCP 传输统一) | **已签核消耗** —— owner「写入治理」(随日耗令);dev 会话据此写 AGENTS.md §五.9 + 本文件两行。**常态不变**:治理面写权归 Fable 5 规划会话(AGENTS §五),本次为 owner 点名例外,下次仍须停手申报 |
| 一次性治理写权授予 dev 会话(2026-07-30 ACP) | **已签核消耗** —— owner 2026-07-30「写治理」;dev 会话据此写 `.loop/state.md` + `.loop/backlog.md`(新卡 ACP-LEDGER-1 / KIMI-UPSTREAM-1)+ `docs/dev/tech-design.md` 指针行 |
| 改 AGENTS.md §三红线 / 降任何基线 / 改对外契约语义(REST `/api/v1` · MCP wire) | 须 owner 签核后才动 |

## 未固化教训

- **`git worktree add` 不继承 gitignored 内容,委派 briefing 必须自包含**(v0.9.15 实锤):派进 worktree 的 codex 子会话(PLUGIN/SURFACES)两个都报告 `docs-local/versions/v0-9-15/*` 在它们的工作目录里「不存在」——worktree 是从 git 对象库检出的,gitignored 文件从来只活在**某一个**工作目录的文件系统里,新 worktree 天生看不到。两个子会话都靠 prompt 里内嵌的完整 spec 正确交付,证明了纪律本身有效,但这是运气好(我把 spec 写得够全),不是必然。**同形待扫**:任何要点名 `docs-local/`(PRD、W0 探针、decisions)的委派 briefing,若目标不是主工作树,要么把关键内容**转述进 prompt**(本次做法),要么显式 `cp -r` 把该目录同步进新 worktree——不能假设子会话「读得到」。
- **secret 经子进程 env 传递 = 默认被子进程的子进程继承,必须读一次即删**(v0.9.15 D19 实锤,W0 真机探针发现,非设计阶段能预判):tech-design 草稿早就写了「secret 只在 host 插件进程 env / 内存,不得进 sandbox bash」,但**没写怎么保证**——真机探针 #19 一测就漏:`CCTEAM_MCP_BEARER` 留在 `process.env`,DSH 自己的 bash 工具子进程(Node `child_process` 默认继承父 env)照单全收。修法是纪律不是补丁:插件 `apply()` 读一次该 env 存进闭包变量后**立即 `delete process.env.X`**,发生在 boot 阶段、早于任何可能触发子进程的 turn。**同形待扫**:凡「把只想给某一层用的密钥放进 `process.env` / 子进程环境变量」的地方,都要问「这个进程还会再 spawn 别的子进程吗?它会不会也看见」——env 是继承性的,不是作用域性的,default 是泄漏不是隔离。
- **「把凭据交给 vendor」不等于「vendor 用了它」——契约之外的东西只能测量,不能断言**(v0.9.14 三连实锤):`mcp_config.rs` 里写着「受管会话用自己的 principal 压过同名全局条目」,claude(`--strict-mcp-config`)与 codex(per-thread 深合并)成立,**ACP 面根本没有同类物**;真机 4/4 受管 grok 装载的是它自己 `config.toml` 里的同名条目,而 daemon 侧只看到 `tier=enroll:…`。更刺的是隔离复现里**注入方 3/3 获胜** —— 配置内容、cwd、plugin-dir、argv[0]、inbound policy、daemon env 全复制都没能重现生产的失败,这本身就是结论:**ACP-vs-config 优先级不是契约**。改法不是继续在 vendor 的地盘上找开关(六个 compat env、两级 `[plugins].disabled`、`--plugin-dir` 遮蔽先例全部实测失效),而是①把 outcome 变成**记录**(principal 首次使用;两条路都没到才 `identity_degraded`,broadcast-only 不入 chat)②换到 ccteam 说了算的战场:**进程血缘**(daemon 亲自 spawn 的子进程,pid 在第一个握手字节前就登记,loopback peer 经 `/proc` 上溯)。判据:**凡「我们把 X 交给 vendor 并假定它会照做」的地方,都要能回答「怎么知道它做了」**。
- **同一个资源在写路径与读路径被当成两种东西 = 一半的修复**(`b6a2b87d` 实锤,「同形扫一遍」的又一战果):codex app-server 重启后,写路径把连接当**可重建资源**(`client()` 重拨 + `ensure_thread_loaded` 重挂线程,agent 照常提交、照常 push),读路径却把 `events()` 当 spawn 时一次性拿到的**事实** —— 于是会话在每块面板上 🔵 working 了 2.5 小时,每个 turn 都死在上游,父会话等一个永远不会来的完成。修在契约(`events` 可重复调用 + `event_attachment()` 无默认实现)而非某个 adapter,故第七个 vendor 必须表态。**扫法**:找「spawn 时取一次、之后一直用」的句柄(`let mut events = …`、缓存的 client、缓存的 socket),问一句「承载它的东西死了会怎样」。
- **ccteam 自己的自愈动作可能就是病根**(`573819ba`):`prime_tool_surface` 发的 `mcp_reconnect` 让 claude **重新解析** server 列表且不再尊重 `--strict-mcp-config`,于是受管会话在启动发现期用自己的 principal 认证一次(所以「从未用过 principal」的守卫沉默),此后整生命周期以机器身份说话(现场 8/8 tool call)。教训:**给「修复动作」也写一条 before/after 的实测**,尤其是那些「重连/重载/重建」类动词 —— 它们通常等价于「重新走一遍初始化」,而初始化的入参未必还在。
- **vendor 的能力边界会变,所以判据要接到 vendor 自报的信号上,别接到硬编码表**(`7a58989c` 实锤):`outlives_turn` 用 `task_type` 白名单推断「这任务是不是后台的」,claude 把 `Agent` 改成异步启动那天,这张表就开始撒谎——而且是**沉默地**撒谎(每个异步 agent 被 turn 结束的安全网清掉,`/status` 里凭空消失,`local_bash` 恰好在表里所以后台 shell 还活着,更像"只是 agent 有问题")。改法不是往表里再加一行,是换信号源:claude 每次变更都重发全量 `system:background_tasks_changed`,认它。**同形待扫**:凡「我们推断 vendor 的内部状态」的地方(任务是否后台 / turn 是否在飞 / 会话是否 idle),都要问一句「vendor 是不是已经在说了,而我们没听」。
- **同一个提交里新增的机制,要先问「已有的行能不能承担」**(`652e2d38` 收 `e541e557` 的矛盾):为了关掉死 turn 的忙窗新加了 `chat_turn_failed` 事件种类,同时又把 `TurnFailed` 折进共享终结记账投影——于是一个失败 turn 写**两条**终结行,自带的测试还断言了与实际相反的行为。判决是**净减**(删掉新加的种类,让共享记账行当唯一终结边界)。教训:加 schema 种类前先枚举既有终结路径;新种类与既有投影**同时**覆盖同一事件 = 双写,必然有一处断言是假的。
- **「构建成功 ≠ 已部署」:修复的验证必须核到运行中进程的 build sha**(2026-07-31 rob 租户 `/status` 实锤):「同一 bug 修过两次仍复现」的病根不在代码 —— 48bd3c81/e6fbef72 正确且测试绿,但从未接管活 daemon:①本机 `CARGO_TARGET_DIR=/data/.local/cargo-target` 重定向产物,部署软链 `~/.local/bin/ccteam → repo/target/release/ccteam` **生来断链**;②daemon(efce019,Jul-29 起)从未重启,`/proc/<pid>/exe` 指向已删旧映像;③PATH 前列还有 Jul-2 旧拷贝 `/data/.local/cargo/bin/ccteam` —— 三个 binary 三个真相。纪律:声称「已修复」前必对齐 `ccteam --version`(磁盘)与运行中 daemon 的 build(`/proc/<pid>/exe` 拷出来跑 `--version`);本机部署 = **实体拷贝**安装,禁软链构建产物。产品化防复发 = DEPLOY-DRIFT-1(daemon 外显 build sha + doctor 漂移告警)。
- **vendor 报告的 turn 结局是数据,静默降级为「成功」= 契约腐蚀**(`410647d` 实锤,建议升为 adapter 契约条目):ACP `session/prompt` 即使 turn 被拒绝/截断/取消,返回的仍是**成功的 JSON-RPC 响应**,`stopReason` 是协议里唯一说明结局的字段——而它在全仓从未被读(grep 只命中测试 fixture)。后果不止 UX:半截前言当最终答案落 `turns.jsonl`,委派 parent 被告知任务完成(`vendor_error:false`),即**账本与完成通知同时说谎**。修在共享 `acp/` 层(`AcpStopReason`)故新 ACP vendor 自动被覆盖(符合「新入口自动覆盖」判据);缺失字段必须判 clean(否则回归 grok/opencode)。**同形待扫**:凡「vendor 给了结局字段而我们只取内容」的消费点(claude `subtype`、codex `will_retry` 已处理;其余 adapter 待核)。
- **「turn 静默」判定必须锚真实起点,arm 在 submit 就会错怪排队者**(`410647d` 实锤,「同形扫一遍」判据的直接战果):`turn_started_at` 早已只认 `Started`/canonical `TurnStarted`,但 `watched_turn`(静默 watchdog)仍在 submit 无条件 arm ——**同一形状修了一半**,漏的那半让 kimi FIFO 里排队的 turn 5 分钟后收到「turn 静默,建议 /stop」,替前一个 turn 的静默背锅,正是用户读成 STUCK 的来源。教训:改「turn 是否在飞」的判定时,把所有读该状态的消费点一并列出(`turn_started_at` / `watched_turn` / `latest_activity` / `visible_events`),别只修触发点。
- **vendor 缺陷的诚实边界 = 不耦合它的私有布局**(kimi 0.29.x,owner「不要硬修」判据落地):kimi 把 `turn.ended reason=failed` 映射成 `stopReason:end_turn`,429 错误只进它自己的 RotatingFileSink 日志(stderr 只有 node warning,实测 2 行)——真相存在,但只在 `~/.kimi-code/sessions/wd_*/session_*/logs/` 这类**非契约面**。去解析它能立刻「修好」现象,代价是耦合上游随时可改的私有文件布局。判定:**不做**,写进适配器头注释 + 立 watch 卡(KIMI-UPSTREAM-1),诚实信号交给 watchdog(现已带 turn 时长 + 最后活动)。同理 `ctx —`:kimi ACP 面不上报 window/token(`usage_update`/`session_info_update` 只在其 schema、从不发出),编个百分比就是造数据 → 改为 `/status` 说明原因。grok/opencode/claude/codex 均正常上报,已审计。
- **by-sid 门后的测试必须真实例化会话**(v0.9.11 wave 修复实锤):`074e284f` 把 `/sessions/{sid}/*` 全量走 sid→project 解析(live map → meta.json 扫描)后,凡「凭空 sid 打门」的 fixture 确定性 404 —— ACL 判正确、fixture 是病根,修法 = 补真实 spawn(HTTP create 先行),不是松门;同形已扫全 `crates/ccteam-web/tests/` 无他例(auth_test 的 no-gateway 探针是锁死契约、非侥幸)。改 by-sid/归属类门时先跑 `sessions_api_test` 族。
- **fixture-only 验证限界**(ae24cb3 review 实锤):fake 只证「客户端实现了 fixture 定义的合同」,不证 vendor 真机行为——真机 grok idle interject 返 `queued` 且**自发 turn** 自答,代码嗅探的 `-32000` 臂是死代码,P0 丢答案由**真机冒烟**揭示(fake 恒绿)。新 vendor wire 行为面收口前上真机冒烟(与 A2A-OBS-3 manual-gate 同旨);冒烟脚本模式:纯 stdio JSON-RPC 直驱 vendor binary,留痕 idle/mid-turn 两态响应形状。
- **vendor 容量中断 = 委派链故障模式**(v0.9.9 FIX1 尾段 codex「model at capacity」,turn 断在门禁前):恢复路径 = `session_collect` 读账本中间记录 → 接手方按其结论收尾,不重做已完成的归因;工作品外部化(worktree/commit)= 会话可弃性。**产品侧主体已固化**(owner 复盘驱动,`2a2b38a`:TurnFailed/终态 Error 贯穿 DelegationSignal,通知冠 VENDOR ERROR = 修「假成功」;TurnStarted 刷 last_active = 消挤停误排);余量 = A2A-OBS-5/OBS-2 卡。恢复纪律候选固化 → verify/README 运行纪律。
- **「同机同红」stash 对照只证「非本 diff 所致」,不证「环境态」**(HERM-1 ① 误归因复盘:对照基线 origin/dev 当时可能已含回归;且「CI 绿」快照会过期)——跨环境同断言复现 = 优先判真回归;flake 归档必须记录首见 CI run 边界,定期复核。
- **委派卡「单 commit 含窄写回」⇒ 卡面 sha 无法自引用**(MCP-CULL-3 卡面 9638ce9 vs 实推 9c2a89e,amend 后漂移,规划 review 校正):委派 brief 应改为「实现 commit → 写回 commit 分离」(与规划自身的 loop: 收口同构),或规划收口时校正 sha。
- **规划自身教训**:backlog 批量卡片删除禁用 sed 范围盲切(v0.9.9 蒸馏时 sed 端点被此前 Edit 吃掉的卡头坑掉整段,靠 cp 备份 + Edit 精确重建恢复)—— 结构性 `.loop/` 编辑一律 Edit 工具 + 事后 `writeback.sh`。

## 流程速查

- **冷启动三读**:AGENTS.md(harness 自动加载)→ 本文件 → `.loop/backlog.md` 文件头 + 所取卡;代码按卡面坐标按需读,不做全仓扫描。
- **收口**:`cargo fmt --all` → 改动面门禁(地图 `.loop/verify/README.md`)→ `.loop/verify/writeback.sh`(队列结构校验)→ commit(英文)→ push `dev`(**main 不直推**;dev→main PR 攒版本,owner 合并)。
- **停止条件**:DoD 达成 → 收口报告 · 需越卡面授权 / 撞人工门 → 停手偏差申报 · 同一问题三次修不好 → 如实报告停(**禁伪造绿**)· 预算/上下文将尽 → 落盘暂停续跑。
