# ccteam 版本蒸馏史(`.loop/history.md`)

> 每版一行,ship 时由规划(控制)会话回填;只留「可指导未来的」,详档 = `docs-local/versions/v0-X-Y/`
> (gitignored 本机)+ `git log`。root README 不含版本进展(§三红线)——本文件是 repo 内唯一版本时间轴。

- **v0.10.4**(2026-08-28 发布:PR #9 merge `16f02edf` → tag → release;fork Actions 不触发,linux-x64 资产手工打包):Telegram 命令响应 + 「Командир」编队按钮(Codex 回落)+ evolution 闭环 + **原子化项目退役**(durable RETIRED 标记 → 内存围栏 → 停会话 join 泵 → 项目级 drain → 清 progress → config 最后删;slug 永不复用;`ProjectRetireError{marker_committed}`);四轮 adversarial review 45 条实锤全修(锁内 flock / 手误烧 slug / 僵尸会话 / 全局 drain / 委派双投 / 旧 lock 占 slug / unknown fail-open / doctor exit code / web worker 阻塞);投影空快照旧病根。门禁:check/test/web-check/perf-gate 绿,2279 passed,仅 `ws_*` WSL2 族红。方法论:Codex 主控死亡后由 Fable 5 从 `~/.codex/sessions` rollout 恢复上下文接手;五波 opus/fable 执行者按文件冲突域并行 worktree,每波前 adversarial workflow 定向
- **v0.10.3**(2026-08-19 发布,合并 v0.10.1/v0.10.2/v0.10.3 三周期):DSH 会话一元化(operator 真 `~/.dsh` 插件注册豁免 + 退役 per-hire 子进程/per-sid `DSH_HOME`,connect 代替 spawn)· 一个身份一个 DSH 配置家(D26 turnkey 种子 + D28 refresh-if-unmodified)· v0.10.2 web 小改批(时间戳/拓扑折叠/导航/DSH iframe keep-alive)· RESTART-1「一 sid 一 body」· TG 三线(TG-STOP-1/TG-FMT-1/RU-I18N-2)。详 `docs-local/versions/v0-10-{1,2,3}/`
- **v0.10.0**(2026-08-16 发布:owner 把 v0.9.15 周期改名 v0.10.0——DSH 第七 vendor +「DSH web 一等公民」两块并作一版发布;owner「pr 改完版本号之后你直接合并。然后发版」→ ship gate `a5cdc8bc` → PR #180 改名「v0.10.0 cycle」→ CI 首两跑各独立触发同一族 gateway echo 测试竞态(`FakeAdapter::events()` 跨身份共享 `Notify` 的纯测试替身竞态,`gateway.rs` 零改动,登记 verify/README env-flake 族)、第三跑三 job 全绿(run 31916013891)→ **merge commit `76860415`**(非 squash,`gh pr merge --merge`)→ main CI 绿(run 31916139457)→ annotated tag `v0.10.0`(`d1f2d51`)→ release run **31916252550** success,四平台 tarball + SHA256SUMS,`/releases/latest` 已指向;**产物实测**:下载 linux-x64 → `sha256sum -c` OK → 跑出 `ccteam 0.10.0 (7686041)`(build sha 对齐 merge commit)+ static-pie stripped)**DSH(DeepSeek Harness)第七 vendor,插件即集成**:官方 harness 无产品化自动化面、无全局 MCP 配置缝,ccteam 顺着它「Cordis 插件是第一公民」的架构做集成——`plugins/dsh-client/` 一个 npm 包双面孔(工具面 8 原名工具 + 传输面仅受管自绑 stdio serve ACP),`execution/dsh_acp/` 坐共享 `execution/acp/` 零新协议机器,`session/load` 冷 resume 真做(SIGKILL 真进程后同 sid 原文召回密码,非豁免)。**DSH web 一等公民**:ccteam web 新增顶级「DSH」菜单,daemon 开一个伴生端口把每个登录身份代理到它自己的 DSH web 实例——operator 见真 `~/.dsh`(attach-if-detected 绝不与手起实例双写同一 home),每个租户一份 `$CCTEAM_HOME/runtime/dsh/web/<user>/` 隔离空间(合并式物化,保留用户自装插件层,凭据出厂为空走 DSH 原生 Settings→Models 自助配置)。**上线后真机 dogfood 当场抓出三个跨过全部单测防线的 bug,一次说透**:①`start_for` 的「已在跑」快捷路径持有 `tokio::sync::Mutex` 的 guard 横跨一次 `.await` 去调用同样要拿这把锁的 `status_for`——不可重入锁自己等自己,一次点击就把 daemon 全部 DSH 相关请求锁死到天荒地老,回归测试补的是「重复 start + 之后 status 仍必须在 5s 内返回」而非「start 一次成功」;②租户 profile patch 生成器给已经在 `dsh.profile.bundles` 里、自带 patch 层会插入同 id 行的 `@ccteam/dsh-client` **又 insert 了一次**,Cordis 直接以 `duplicate loader entry id` 拒绝整个 boot——DSH 的 patch 语义是「带 `insert` 是插入,不带 `insert` 但带 `id` 是覆写」,且插件 `config` 字段是**扁平**的(直达 `apply(ctx,config)`),嵌套进命名空间会让 enrollment 静默失效;两处修复都把原先「子串断言」的测试改成「结构断言」,因为 bug 恰恰是「子串都对、结构不对」;③`crypto.randomUUID` 是仅安全上下文可用的浏览器 API,原生 `dsh web` 绑 loopback(天然安全上下文)从不出事,而伴生端口把它带到 LAN 明文 HTTP 上就整体失灵(选工作区、模型目录、每一条 RPC 全部 `crypto.randomUUID is not a function`)——修法是**ccteam 是拿走这个安全上下文的一方,所以 ccteam 补回平台 API**:代理层把 HTML 响应缓冲(仅 `text/html`,资产/RPC 照常流式),用仍可用的 `crypto.getRandomValues` 现算一个真 RFC4122 v4 UUID 注入 `<head>` 最前(原生已有则整段 no-op),同时把 `Accept-Encoding` 剥掉保证响应体恒 identity 编码、拼接不会撞上压缩流。SPA「DSH」页(状态头 + 满幅 iframe + operator 专属仅 loopback 展示的「打开原生窗口」按钮)由规划会话亲自设计实现(owner「codex 审美不行」)。三个 bug 全部真机复现 + 回归测试锁死(裸 socket 直连源 + 独立 axum/reqwest 复现代码排除通用陷阱、精确定位到 ccteam 自己的锁/patch/安全上下文逻辑),operator 与租户双身份端到端重验。部署顺带踩中一个自造的坑:`web/dist/` 是跨 cargo profile 共享的磁盘路径,debug profile 的 `cargo check/test -p ccteam-web` 若带 `CCTEAM_SKIP_WEB_BUILD=1` 会直接把它覆写成占位页,而 release profile 的下一次 `cargo build` 未必重触发 build.rs——教训:**这个 env var 只该在整段调试会话里从头到尾要么用要么不用,绝不能跨 profile 交替**,已作为部署纪律记录。基线 1887→**1895/0**(`--lib --bins`)/ clippy 0 / fmt 干净 / vitest 612→**622**(56 文件)/ 新增 `dsh_web_test`(3,含死锁回归)+ `dsh_acp::materialize` 结构断言(13)。
- **v0.9.14**(2026-08-12 发布:owner「把 dev 分支上的内容,都 release 流程」→ ship gate `da80ecc0` → PR #177 CI 三 job 绿(干净环境仲裁 1864/0)→ **merge commit `fb2a9540`**(非 squash)→ main CI 绿 → tag `v0.9.14` → release run 31518460712 success,四平台 tarball + SHA256SUMS,产物实测跑出 `ccteam 0.9.14 (fb2a954)` + static-pie;尾随 owner 直驱的 vendor 前置安装卡与 Pi 链接修补经 PR #178 `92fe0c3f` 追平 main,不重发版本)**身份从「一台机器一个」变成「一个进程一个」**。病根一句话:五 vendor 全局配置里的那枚 admin web token 是**每个由该 vendor 启动的进程**共用的身份——所以手起的 agent 当不了委派父(子会话全挂成根)、没有自己的 project,而两小时前在别的仓库跑的另一个进程与它**逐字节同一个 caller**。三层重建,每层都刻意选了「让下游零改动」的形状:①全局注册改写 **enrollment 凭据**(`ccteam-enroll:<id>:<secret>`,只说明「这份配置是谁的」、自身零授权;legacy `Bearer ccteam:<hex>` 与 stdio `command` 条目一律读作**未注册**,daemon 幂等修复替换,pre-v1.0 不写迁移码;writer 顺手拒收非 enrollment bearer,否则 predicate 恒判未注册 → 每次启动无限重写且日志无解释)②**per-process 身份在 `initialize` 签发**——Streamable HTTP 本就有 `Mcp-Session-Id`,写代码前先对五个 vendor 真机验过回显(claude/codex/grok/opencode/kimi 在 `notifications/initialized`、SSE GET、`tools/list`、`tools/call` 全回,codex 与 grok 还会发关闭 `DELETE`),故不是押某一家的合规性;binding 开 → 铸 ledger 节点 → mint per-session principal 并 **promote**,于是 `session_spawn` 走的仍是既有 Ambient 路径,`_caller_sid` 是真的,子会话真的挂在它下面;id **不是**凭据(每次必须与凭据同时出示,binding 只对开它的凭据解析,泄漏的 id 重放什么也不是),陈旧 id 一律同一个 404 让客户端 re-`initialize`(transport 自己的恢复路径,不是 ccteam 发明的)③**enrolled client = 账本一行**(`managed_by: external`,同一 sid 命名空间/委派树/meta 形状)**但不进 gateway live map** —— owner 拍板的 Option B:那张表的含义是「ccteam 握着一个线程」,挤停/预算/工具面 prime/事件泵都在迭代它,`thread: Option<…>` 方案要守 39 处 `.thread`(gateway.rs 24 处),漏一处就是 ccteam 去「优雅停」一个它根本起不了的进程;`vendor_uuid` 故意留空(编一个会让日后 takeover resume 错会话),不认识的 `clientInfo.name` 不强塞 vendor。**删除比新增更重要的三处**:MCP 数据面**无 admin 层**(web token 在 `/mcp` 401 并点名它认的两族凭据——它是控制面 `/api/v1` 的凭据);stdio `internal mcp-serve` 连同 orphan/PDEATHSIG/idle-timeout 关停机与 forwarder **整体删除 1.6k 行**(「还能跑的命令」= 陈旧配置仍能调用的第二条身份路径);**猜测**被删除(daemon 默认项目 + lazy scratch 兜底,与 2026-07-28 聊天路由那次是同一个病根、这次补上被漏掉的入口):没依据就拒绝并列出可选 slug,凭据没钉 project 的 caller 必须自己点名(`can_see_project` 判,首次点名终身绑定,不可见与不存在逐字节一致)。**最值得记住的是「不要在别人的地盘上打身份仗」**:受管 ACP 会话的 per-session 凭据只是**给 vendor 的一个 offer** —— 真机 4/4 受管 grok 装载的是自己 `config.toml` 的同名条目,而隔离复现里注入方**3/3 获胜**且六个 compat env、两级 `[plugins].disabled`、v0.9.13 那个 `--plugin-dir` 遮蔽先例全部实测失效;结论不是继续找开关,而是①把结果变成**测量**(principal 首次使用记录;两条路都没到才 `identity_degraded`,broadcast-only、永不致命)②换战场:daemon 亲自 spawn 的进程,pid 在第一个握手字节前登记,loopback peer 经 `/proc` 上溯回它所属的受管会话。同族还拔掉一处自伤:`prime_tool_surface` 发的 `mcp_reconnect` 让 claude 无视 `--strict-mcp-config` 重挂全局同名条目(实测 8/8 tool call 落 enroll tier,而启动发现期那一次认证成功恰好让「从未用过 principal」的守卫沉默),故 rebuild 整体退为 `RespawnRequired` —— 「没有工具的会话」比「顶着别人身份的会话」问题小。**第二条主线:读路径也是可重建资源**(`b6a2b87d`,一个 codex 会话在每块面板上 🔵 working 了 2.5 小时、每个 turn 都死在上游):写路径早把连接当可重建(重拨 + 重挂线程),读路径却把 `events()` 当 spawn 时的一次性事实——修在契约(`events` 文档化为可重复调用、`event_attachment() -> Rebuildable|OneShot` **无默认实现**,terminal 协议 = `OneShot`),泵 250ms→60s 退避重连、封顶但**永不放弃**,两端各记一行(`session_stream_detached` / `session_stream_reattached{gap_ms,attempts}`),断连时敞着的 turn 走**与 vendor 自报失败同一条**路径合成失败(兜底不写账本、不通知父会话,就等于没有兜底),codex `turn/completed` 也是失败的上报方式故 `codex_turn_outcome` 显式判、无 `Ok` 兜底。**第三条:一个撕裂字节让 120 MB 账本整份读空**(`bd3b9990`)—— `read_to_string` 全或无,而每个调用方都把 `Err` 降级成「没有事件」,于是 42 万行完好记录被丢弃、整个项目每个会话读成 `idle`、成本报 `$0`;修的是**读的形状**(`fs_atomic::read_jsonl` 按 `\n` 切、逐行容错,四个 append-only 读者统一走它),外加「无证据」不再铸 `idle`:`classify_session_activity` 成为唯一解析器(文件判据 ⋈ daemon 在飞 turn,**单调**),IM 与 web 因此不可能再各说各话。另:`0d0a2ba8` 受管会话 MCP endpoint 一次解析四方言(`/new pi` 在健康 daemon 上死于「靠祖先进程 env 传 URL」)· `c1ced789` principal 搬出会话记录并自带一把锁(vendor 在 `start_thread` 里就用凭据握手:opencode 每次 spawn 烧满 30s、pi 直接死锁)· `2e41a468`+`30668ab7` 团队页空字段三修(`observed_model` 只读展示 / 停止会话读盘解析 / 无价表 vendor 回落 token 数,绝不假 `$0.00`)· `8da64b83` release notes 与 install.sh 不再声称 ccteam 建立在 tmux 上(stream-json 成为默认那天就不真了)· README/usage 增补「先装并登录至少一个 vendor CLI」(owner 直驱,六 vendor 安装链接 + 登录命令按本机实测写)。基线 1804→**1864/0**(`--lib --bins`,7 target,CI 干净环境同值)/ `make test` **124 bins 2638/0** / vitest 606→**612**(55 文件)/ ccteam-web 全量 379+3(已登记 `ws_*` env-flake 族)/ clippy 0 / fmt 干净
- **v0.9.13**(2026-08-08 发布:owner「拉最新 dev,走 release 流程。正式发布」→ ship gate `b66ffbd1` → PR #176 CI 三 job 绿(干净环境仲裁 1804/0)→ **merge commit `3a77bbe4`**(非 squash)→ main CI 绿 → tag `v0.9.13` → release run 31259214102 success,四平台 tarball + SHA256SUMS,产物实测跑出 `ccteam 0.9.13 (3a77bbe)` + static-pie)**Pi = 第六 vendor**(`execution/pi_rpc/`,`pi --mode rpc` 长驻 stdio JSONL;四 wave:adapter core → tool bridge + HITL → 可达性/预算/probe/工厂接线 → web 六 vendor UX)。这版最值得记住的不是「又接了一家」,而是**差异住在哪**:pi 是唯一不让 ccteam 写它配置的 vendor,于是差异被表达成注册表里的一个字段 `AgentProbeSpec.tool_surface = ManagedSessionBridge`(工具面 = spawn 时挂 ccteam-owned 扩展),而不是散在五处分支;「只能本机跑」同样写进类型 `HostExecutionScope::LocalOnly`,跨机门对每个 vendor 穷举 —— **下一个 vendor 落地时,这两处会强制它表态**。role 走用户 `.claude/agents/<role>.md` **body 原文**投射(sha 记账,frontmatter `pi.model` 须 canonical `provider/model`);HITL 复用共享 approve/deny 路由;强度轴**按模型**(`thinking_level_map` 的 `Some(None)` = 该模型不支持此档);计价只认 vendor 自报 USD(同 opencode,不套别家价表)。**paneless turn 全生命周期进 `progress.jsonl`**(`e541e557`):病根一个、症状两个 —— paneless 协议只镜像 turn **末尾**,submit 到完成之间最新行还是上一轮 `chat_turn_completed`(idle 边界),于是父会话轮询读到 idle/stale,`session_collect(tail)` 把**上一轮**答案当新答案交出去。三边界都补在「层」上:开 = `after_turn_submitted`(所有提交路径的唯一 choke point,故新入口自动继承;`Queued` 也算忙)· 心跳 = 泵内 60s 节流、判据 `is_terminal()` 而非点名 vendor · 关 = 三个终结事件都落行(`build_chat_turn_running_long_event` 补 `sid` —— 它本来对自己不可见,还会经 project-tail 兜底泄到兄弟会话)。随后 `652e2d38` 收掉自己制造的矛盾:`chat_turn_failed` 与共享记账行**双终结**,判决 = schema **净减**一个事件种类,一个失败 turn 恰一条终结行。配套 `4c433f22`(ACP-LEDGER-1)让失败/截断 turn 的 usage 跨 vendor 入账 —— 失败照样计费,账本写零就是撒谎。**后台任务归属改认 vendor 快照**(`7a58989c`):claude 的 `Agent` 已异步化(活过启动它的 turn),而 `outlives_turn` 还按硬编码 `task_type` 表推断,turn 结束的安全网把每个异步 agent 都清了(`local_bash` 在白名单里 —— 所以后台 shell 活、agent 死,正是 `/status` 「明明在跑却看不见」的病根)。改判据来源 = 消费 claude 自报的 `system:background_tasks_changed` 全量快照,**快照只收窄安全网、永不停用它**(掉出快照 = 重新变回 turn-scoped);写规则前先真机探针量了两种任务的时序并把它钉进测试。同族第二修 `e12292a7`:`/status` 静默基线取 `max(turn 开始, 最后事件)` —— 刚发出的 turn 顶着上一轮的陈旧事件秒读 STUCK,是把跨 turn 的量当成 turn 内的量。**受管 grok 不再继承 Claude 插件的 MCP server**(`fd341f49`):每个受管 grok 拖一个 `bun …/telegram` 子进程去抢用户 bot 的 `getUpdates`(Telegram 只许一个消费者)—— 与 ccteam 自己的 IM 网关对打,外加每会话一个孤儿。这是 `GROK_CLAUDE_MCPS_ENABLED=false` 那个决定漏的**第二道门**(env 只关 `~/.claude.json` 扫描、不关 plugins);真机对 grok 0.2.118 把六个 compat env、项目/用户两级 `[plugins].disabled`、`CLAUDE_CONFIG_DIR` 全证伪后,解法 = `--plugin-dir` 传同名空插件让真插件输在名字碰撞上,判据「声明了 MCP server」而非名单(新插件零改码被覆盖)、全程 fail-open。web 四改(sidebar 工作区 **⋯** 菜单 = 项目移除/新建会话/复制路径 —— 项目管理回到项目所在处,Ops 的项目面板同 wave 建成又拆掉;Access「我的 IM bot」每平台一张两步引导卡,保存后自动开始捕获发送者;Ops 每主机 harness 管理卡,按钮只出现在后端真会接受写入处;markdown 表格包进滚动容器不再撑破气泡)+ 中立 `routing.md` 起手模板(删预设角色/模型样表 —— 写死一次就会过期并撒谎,分工留给用户)。基线 1757→**1804/0**(`--lib --bins`,7 target)/ `make test` **123 bins 2564/0** / vitest 566→**606**(54 文件)/ ccteam-web 全量 351+3(已登记 `ws_*` env-flake 族)/ clippy 0 / fmt 干净
- **v0.9.12**(2026-08-02 发布:owner「把最新的积累在 dev 分支上的发 release」→ ship gate `1d8c29a0` → PR #174 CI 三 job 绿 → **merge commit `0c953f38`**(非 squash)→ main CI 绿 → tag `v0.9.12` → release run 30715055386 success,四平台 tarball + SHA256SUMS,产物实测跑出 `ccteam 0.9.12 (0c953f3)`)**spawn 调参轴打通(model + effort)**:病根 = 三个 spawn 入口三种主张(REST 给 grok+kimi 抹掉 effort、MCP 只抹 grok、IM 根本带不了),且抹掉后照样返 201/live sid —— 显式选择静默变 vendor 默认、无处可察。修在 `SpawnTuning` 单点(**per-vendor 臂全删**,REST mapper 留 `_vendor` 参数专供回归测试证明映射与 vendor 无关),下发按协议族:claude `--effort` / grok `--reasoning-effort`(自报 `low|medium|high`,真机验证会话起来就报所给档)/ codex sticky 首 turn override / ACP `session/set_config_option` —— 且**按 spec category `thought_level` 认轴而非某家 id**(kimi 叫 `thinking`、opencode 叫 `effort`,只认后者正是 kimi「跑在 high 却报无强度」的病根,新 ACP vendor 自动被覆盖);拒绝策略单点 `spawn_pick_refused`(点名了就必须给到,否则报错;没点名才吃默认;冻结的 terminal 协议无强度轴 → 显式拒绝并指向 stream-json)· 持久化补最后一层洞(`meta.json` 有 `model` 无 `effort` → 三条 re-spawn 路(resume/`/role`/重启重建)一律硬编码 `None`,即「显式 effort 只生效一次」;`/role` 重取 role 的 model 但携带会话的 effort —— 强度属会话不属角色)· 发现面三家一源:`GET /api/v1/models`(vendor 自报目录 + 强度梯 + `observed_at`/`source` provenance;从未观测 = 空列表 + null 而非「它没有模型」,**advisory 永非白名单**)· MCP `status` spawn 配方内联两轴 · IM `/new … model=/effort=`(order-free k=v + `m=`/`e=` 别名,历史 `/new` 形状逐字节仍解析,错键报错不再被当 role)· web composer 按 vendor 渲染真菜单(`ComposerDraft.effortKey: EffortKey` → `effort: string` vendor 原文,全局梯 `EFFORT_KEYS`/`wireEffort`/`effortLabel` 整族删除无 shim,`normalizeDraft(draft, catalog)` 单一有效性闸;没有强度轴的 vendor 干脆不渲染该段)· 团队拓扑「模型·强度」列 + `@container` 响应式树(按画布宽而非视口折行,1600→320px 无横向溢出)· 强度显示统一为 vendor 自己的 token(`高`/`极高` 是任何 CLI 都不收的词,zh/en 由构造同一)。**上下文口径修正**:`ContextUsage.used_tokens` → `Option<u64>` + `ContextSource{Reported,Derived,Probed,Unknown}` —— 三个 ACP adapter 各自把「窗口已知/占用未知」压成 `0`,而这正是 resume 后的常态,于是每个 grok 会话重启后都显示 `ctx 0/500k (0%)`(占位符冒充测量值比没有测量更坏;`totalTokens:0` 覆盖真占用是同一谎的第二条路,真机 grok 0.2.114 实测 3ms 返零总数)· ACP status 快照落盘(`execution/session_status.rs`,stream-json 早有的私有解法上提共享层:turn 边界折叠 → 原子写 → live 优先否则读盘,`thread_status` 仍是纯内存读)· **kimi 上下文从「vendor 限制」变成真数字**:它不 *push* 但 *答* —— `status` 是它自己 `available_commands_update` 里公告的命令(0.26.0 实测 15–21ms 本地答、无模型调用),已公告的命令是契约面(而私有 session 日志不是,这条界线正是当初拒绝 log-scraping 的理由);拉取作为 runner 自排的 turn 占用 turn 槽(并发 submit 排队而非抢缓冲)、不发事件(不进 `turns.jsonl`/IM/委派父)、三重篱笆(解析不出保持原值绝不猜 · 排在所有 push/derive 通道之下 · 只发 vendor 当前公告的命令);顺手修真 bug:transport 广播丢弃订阅者出现前的通知 = 整个 ACP 握手,kimi 的命令目录恰在此刻只发一次 → 改为保留预订阅通知并恰一次交给首个订阅者。**MCP 注册全 vendor 全路径统一 HTTP**(claude 是最后一个还写 stdio 的;三条触发路径全 funnel `install_mcp_into` → 整表替换杀 legacy `command`/`args`/`env` + 0600;`claude_mcp_registered` 改认 HTTP 形状故旧条目读作未注册→被自动修复;每会话 curated 配置只剩一种线形(`McpConfigMode` 删除);项目 `.mcp.json` 的 ccteam 写者删除(零生产调用方 + 红线本就只放第三方);terminal 协议靠 `--mcp-config` 手工 scope 保住 per-session principal,不被静态 admin bearer 静默降级)+ 升级修复面(owner 追问「已有 stdio 条目的用户怎么办」→ 真实二进制跑沙箱 HOME 的升级回归测试 + `~/.ccteam/run/mcp-url` 记录 daemon 真实 bind,非默认 `--web-bind` 不再被改成指向空端口)。**install 单一落点阶梯**(治 state 在案「三个 binary 三个真相」):`CCTEAM_INSTALL_DIR` → 已有 ccteam 所在目录(原地升级,软链先解析、`target/{debug,release}` 永不当落点)→ `~/.local/bin`,`make install` 调 `install.sh --print-install-dir` 而非复制阶梯,`ccteam update` 钉住被替换 binary 的目录(`command -v` 答的是另一个问题);装完点名 PATH 上其它 ccteam(只报不删,指向同一文件的软链不算对手)+ 落点非默认时明说;`make install` 盖 `install-channel=source` 免得自建 binary 被当 standalone 悄悄换成发布版;`InstallMarker.bin` additive = 「哪个 ccteam 才是真的」变成存下来的事实。**ACP turn 结局契约**(`stopReason` 从未被读过 —— 半截前言当最终答案落账、委派父被告知任务完成,账本与通知同时说谎;修在共享 `acp/` 层故新 vendor 自动覆盖)+ IM/web 租户面五修(live session 继承 tenant project owner · 每租户 bot fail-closed + IM 保存不再毁数据 · PWA 安装文件根路径匿名(修 mobile-only 404)· 未编目卫星项目保持可见 · fleet 面 owner 门)+ IM 发送者绑定被拒时给出可操作提示;基线 1708→**1757/0**(`--lib --bins`)/ vitest 506→**566**(52 文件)/ clippy 0 / fmt 干净
- **v0.9.11**(2026-07-29 发布:owner「合并pr,tag 和发release」→ PR #171 **merge commit** `efce0196`(非 squash,dev 即成 main 祖先)→ main CI 绿 → tag → release run 30427416169 success,四平台 tarball + SHA256SUMS)团队页驾驶舱重设计(owner 直驱当日 PRD→拍板→落地,PRD `docs-local/versions/v0-9-11/prd-team-page.md` 五项全默认;规划拆 TEAM-1..4 串行子代理派工 + 治理/docs 亲笔):拓扑独占(roster/timeline 两 tab 删,独有信息并入 = 多 host 徽章 + 行尾真链接)+ 会话真超链接(`Link` → `/chat/s/:sid`,右键/中键新 tab 对照父子,`33545de5`)+ per-vendor KPI chips(live+Σcost 点击过滤)+ 最近委派 ticker(客户端 SSE log,零新后端)+ live 节点 model join(共享 `sessions_api::resolved_thread_status` helper 锁外 await,idle 诚实 None,`e6704daf`)· 分工 charter tab(`9609eb37`)= vendor 名册(hosts `AgentHealth` + graph 按 vendor 聚合)+ 宪章编辑器(REST `GET/PUT /projects/{slug}/routing`:GET project|global|none 三态 + fallback 只读回显,PUT 仅项目文件原子写 + 256 KiB cap 413,`project_acl_layer` 形状自动覆盖 + 显式 ACL 测试;全局 web 只读 = owner 拍板)· 编队起手(`670e335f`)= `web/src/lib/playbooks.ts` 6 编队定义唯一家(总控-工班/主力-顾问/交叉互审/并行竞标/调研三角/金字塔用工;Home launcher 与团队页双消费,router-state 一次性起手防刷新重放;内容红线判定:卡文案 = UI 文档性质,成段配方住 hub)· docs 编队目录 9 式(+监工/值守/跨机;orchestration §7 与 usage 团队页节,中英双语)· wave 修复 = sessions_api 两红封口(`b20e1e96`:074e284f fail-closed ACL 判正确,fixture 凭空 sid 打 by-sid 门 → 补真实 spawn,同形扫全 tests/ 无他例)· 追加(owner 实机截图 + telegram 追问「vendor 重复/排列乱」驱动,活 daemon `/api/v1/hosts` 查活实锤:3 条真实登记含一条离线卫星 `smoke-self` hostname 撞了 `local`,v0.8.24 时代真机 smoke 测试残留;产品缺口 = `ccteam host` 无反注册命令)= TEAM-5(`4c45ed01`)core `HostRegistry::remove` + REST `DELETE /hosts/{id}`(local 400、online 无 force 409、unknown 404,与既有 hosts 兄弟 handler 同 ACL 姿态)+ CLI `ccteam host rm` · TEAM-6(`61692685`)分工 tab 名册按主机分组(local→在线→离线排序、离线默认折叠、host_id 恒显消撞名歧义、非 local 主机移除按钮离线一键/在线两次确认)· 三候选批量动工(owner「后选动工,派 subagent opus」,四 opus 子代理串行)= TEAM-7(`8ec9cf2e`)名册卡点击→切拓扑并按该 vendor 过滤(与 KPI chips 同一过滤源)· TEAM-8(`ee32b6cd`)`HostSummary.last_heartbeat_unix` additive 字段 + 名册离线时长外显(≥7 天建议清理,warn 强调;纯展示零自动删;eslint purity 逼出 nowMs 注入式时钟设计)· TEAM-9(`3621e871`)设置 HostsView 收敛为主机接入动作面(删观察网格 578→474 行,留 register-mcp/卫星项目 import/join 指针 + 「→ 团队页」链接;JoinCard 导出原地不动供 AccessView;agent 纠正 brief 错误前提 = JoinCard 本就不内嵌)· TEAM-10(`36c5793a`)npm「可更新」提示迁名册版本行(收敛不丢信号,`vendorLatest.ts` 复得生产消费者;npm 映射覆盖 claude/codex/grok/opencode,kimi 无 npm 渠道诚实无提示)· 线上清理:`smoke-self` 残留已从活 daemon 注册表移除(备份留 /tmp,心跳周期后二次复核无复活)· 周期同车(wave 前 6 commits)= ACL 病根修法(具名 principal `c03b3913` + session 单一归属策略 `074e284f` + 项目解析不越界)+ session rename 全链 `236f8e94` + SSE 401 cookie 自愈 `8d0ede22` + 委派通知与 vendor 失败外显 `bb29c4e4`;基线 1687→**1708/0**(`--lib --bins`,Linux dev 机全绿)/ vitest 436→**506**(50 文件)/ ccteam-web 全量 344 通过+3 已登记 `ws_*` env-flake / clippy 0
- **v0.9.10**(2026-07-26 发布:owner 深夜授权规划执行 merge PR #170 + tag + release)MCP 工具面治理周(外部 agent 反馈两轮驱动):screenshot 工具+关联面全退役(8→7,web 路由/core 渲染管线四依赖/IM `/screen`,−1376 行)· 裸名发现别名 `claude_codex_grok_kimi_opencode_status`(7→8 纯 alias,治「宿主只显工具名」发现失败;名字自 AgentVendor::ALL 派生测试锁死,opencode 殿后 owner 钦点)+ status 配方块 · spawn `protocol` 参数删除(通道纯 vendor 派生 claude/codex=stream-json、grok/opencode/kimi=acp,传入即硬错 `PROTOCOL_SPAWN_PARAM_REMOVED` 镜像 host 先例;QoderWork「grok 强制 acp 藏括号里」踩坑反馈闭环)· MCP-DX 两轮净减法(描述 −792 字符 + vendor 关键词纯文本化 + 可操作 project 错误 + 单项目自动默认 + wait 完成遥测)· doctor 分组版式(五 vendor 每家一行)+ `ccteam start` 启动自动注册五 vendor 全局 MCP(替代手点)· web IA 改版(owner 三项直驱 + 规划钉「接入」设计):market 迁工作流 tab(skills 默认分类排首、项目选择器仅 agent/plugin)+ ops 删 `status-sessions` fleet 大表(拓扑归 团队 视图)+ 新设置 tab「接入 Access」admin-only 四卡(外部 Agent mcpServers 配置模板首次安家(origin+登录 token 客户端渲染)· 卫星 JoinCard 迁入 · 全局 IM 凭据自账号面迁入 · 用户登录链接)· IM 回执「下一步」提示补全(`/model` 透传回执 IM 面尾行 `↓ 查看状态 → /status` + picker 按钮 nav:cd/nav:use 与 typed 同待遇)· 治理:AGENTS.md 上下文工程瘦身 −41%(Anthropic Claude-5 指南,红线逐字保留)+ §三红线增删三条(引擎零 LLM / daemon 无自主决策 / vendor 足迹唯一写)· 委派范式定型 = codex 全程实现(worktree + 两 commit 写回)+ fable5 规划发卡/review/抽查;ship 后同窗四补:HERM-1A restart-restore 竞态根因修复(daemon 恢复期 `/sessions` 读到半程恢复表 → restore 完成 watch 信号 + 仅早到 `/sessions` 停靠独立任务,零 sleep;该红曾被误归因「宿主态泄漏」,stash 对照盲区教训固化 state.md;PR CI 首咬 + 仲裁绿)+ MCP-DX-3 第三轮反馈(beacon 别名改 owner 钦点字面 `grok_claude_codex_kimi` + spawn project 五级默认梯 explicit>cwd>sole>configured(`default_project` 键)>scratch 懒供给 `~/.ccteam/default_project`,响应 `project_source` + scratch 级「先干活后提示」静态 note)+ STATUS-SLIM-1 status wire 瘦身(projects 9 键→`{slug,cost_24h_usd}`+orchestrator 块删除换 `daemon:{status,message}`,PhaseState/MAX_CONCURRENT 考古退役;三活字段深退役按「功能优先」裁决收窄,残量 = STATE-CULL-1 候选)+ HERM-1B 宿主泄漏密封(transcript_tail 根因 = lib 测试突变全局 HOME 并发重定向他测 → 显式根注入缝;roles 泄漏不可复现,加界外诱饵反断言);收口周再追三面:接入页宽屏 + REST API 卡 ACCESS-IA-1(`e2e7850`)· web ACL 收敛 WEB-ACL-1(`1025450`,admin 专属只剩用户管理+全局 bot 凭据,REST/SPA 按 token 身份×项目归属放开,tenant token 自助轮换;§三红线行 owner 签核修订)· **活跃消息 vendor 原生注入**(owner 直驱 codex s131 无卡,`ae24cb3`:adapter `TurnRouting/TurnSubmission` 契约 + gateway 单核心 Inject 路由 + turns.jsonl per-input `input_id`;grok = `_x.ai/interject` 原生中途注入,codex = turn/steer `clientUserMessageId` 回执,kimi/opencode 诚实降级 FIFO;规划 review **真机 grok 冒烟**揭示 P0 完成边竞态丢答案(真机 idle interject 返 `queued` 且自发 turn 自答、`-32000` 臂死代码)→ codex 修 `f3ea8bf` vendor 自发 turn 合成收口 + Started 重盖陈旧时钟;fixture-only 验证限界教训固化 state.md,macOS 宿主两族测试红顺带实锤登记 = TEST-MACOS-1);基线 1651→1669→**1687/0 预期(CI 仲裁;本机 1686+1 已登记 macOS TMPDIR 红)**/ vitest 43 文件 436 / clippy 0
- **v0.9.9**(2026-07-24 dev→main PR 待合并)全局 skill 库(PRD grill-me FREEZE + 规划决议 `docs-local/versions/v0-9-9/decisions.md`,owner 全程委托「review→改良→开发→PR」):`~/.ccteam/skills` 唯一仓根(入 canonical_home;嵌套 id `validate_skill_library_id`;递归 `list_library_skills`;`write_library_skill(_file)`)· hub install 双目标(agent→项目 / skill→库,`install_plugin(project_dir, library_root, …)` 显式注入、项目零写入、installed_status skill 按库三态)· 会话显式 attach(`GET /api/v1/skills` admin-only + turn `{kind:skill,scope:global}` 路径指针 + 远程项目可读拒;composer 两段,全局段 admin-only)· 项目自有面(`.agents/skills` 实体 + `.claude/skills` 软链,ensure/migrate,遗留实体兼容读)· `ccteam skill` CLI 组(add/ls/rm/update + source 整仓登记 `.sources.json` + `role add` 拒 skill + doctor advisory)· A2A P0-1 = inline wait 诚实上限 240s(wire 0..600 不变,超限 240s 处 pending 附 requested/effective/hint;kimi 委派复盘其余项排卡 A2A-OBS-1..4)· 存量清理:P1-3 remove_test 对齐废-cto + FIX1 修 #167 `/newproject` owner 盖章产品 bug(任意路径项目 `/cd` 被拒,双基点归因实测)+ P2-1 CI 增确定性测试 job(`--lib --bins --locked`);编队 = grok 侦察 + codex×2(W1→W2→W4→P0→FIX 链 / W3)+ kimi(W5 SPA)+ fable5 规划总控,codex 容量中断一次由规划按账本中间记录接手收尾 · owner 复盘驱动 P0 双修 `2a2b38a`(vendor 致命错误 TurnFailed 贯穿 DelegationSignal、通知冠 VENDOR ERROR 修「假成功」+ paneless TurnStarted 刷 last_active 消挤停误排;委派工效余量排 A2A-OBS-5)· P2-1 CI test job 首跑即咬出 `session_tool_tests` 隐性 PATH 依赖(开发机恒绿假象)→ hermetic 注入缝 `0ec136d`(per-Gateway 可用性快照,双 PATH 红后绿证,门有牙实锤);基线 1623→1651 / vitest 417 / clippy 0
- **v0.9.8**(2026-07-23 dev→main PR #166)外部 Agent MCP Phase 1(研究稿 `docs-local/research/external-agent-mcp-symmetric-architecture.md` D1–D10 推荐默认,owner 拍板仅 Phase 1):tenant web token 调 `POST /mcp` = 新 `McpCaller::User` principal(绝不映射 Admin/Ambient)+ 全 8 工具 project ACL(unknown/forbidden 不可区分防枚举)+ spawn owner 归 `user:<id>`(修硬编码 `web-api`)+ 身份策略下沉 `ccteam-core::identity` 单源 · 异步可见性三连(owner 实测 s117 事故驱动):web SSE 重连拉 history 权威回填 + visibility/online 复活死流(修超 ring 丢帧 + 90s 永久放弃)/ **root** 会话非亲发 turn 收尾镜像 owner IM(turn 起源标记在 submit 缝,委派子会话不镜像防刷屏,review 裁决)/ IM `/status` 直接子会话外显 + `/sessions` activity 标记(session_list 同源分类器)+ 按钮纯动作面 · v0.9.7 售后五修(update inode 交换 + 版本门 / resume 阶梯 / 关停泵泄漏 / CI 去 rmux / 基线口径 `--lib --bins`);全程 codex 派工(s118/s119)+ fable5 规划把关,基线 1604→1623 / vitest 392→395
- **v0.9.7**(2026-07-22 合 main #165)daemon 生命周期重构 = Codex pid-detach 取代 systemd/launchd(借鉴 codex `app-server-daemon`,attribution 进 LICENSES.md):`ccteam daemon start/stop/restart/status/logs` 全 `--json`(setsid detach + JSON pid 记录 `process_start_time` 防复用 + `daemon.lock` 锁 + ready×managed 双判定 + MCP `initialize` versioned probe + SIGTERM/`--force` SIGKILL 仅 daemon 自身)· legacy unit 一次性自动接管(指纹白名单,手写永不代删)+ install.sh/Makefile 零 unit 生成 + trigger-file 全链退役 · `ccteam update` 按渠道(Standalone 重放 install.sh 无第二下载器 / Source 打印 `git pull && make install` / npm stub V094 / Other 报错)+ 升级重启合同(drain 5min·`--now`·`--no-restart` + 版本核对)+ InstallChannel(env/marker/路径)+ lazy version-check(`state/version.json` ≥20h,curl redirect fetch)+ doctor updates 段 + fleet 版本偏差外显 · README 重构可扫读;owner 直驱,两轮 subagent 撞会话限额由 fable5/opus 规划接手收尾;基线 lib 1497/0(含随车 2 个 ccm 提交:Grok 托管会话不 spawn stdio mcp-serve + idle 会话后台任务 /status 外显)、clippy 0;tag+部署 held(pre-release rc 先行人肉测)
- **v0.9.6**(2026-07-21 合 main)多 vendor 编排发现面:status vendor 面板(probe 下沉 core `host_registry`、daemon-aware caller 收敛)+ advisory 模型目录三源(runtime last-seen `model_catalog.rs` / hub `models.json` / 用户注释,零 spawn 校验)+ routing notes(`<project>/.ccteam/routing.md` 覆盖全局、global 缺失生成不覆盖)+ spawn 失败发现面 · **compare 全链退役**(−1716 行,无 wrapper)+ `is_claude_family`/`model_warning` 删除 · README 吉祥物 logo 入 web(favicon/PWA 全套)· docs 模型路由章 · cct-codex/cct-grok wrapper skill 退役(hub 下架 8024ec4,MCP instructions 原生覆盖)+ 快速开始模板重设(多模型对比/跨 vendor 互审替换 plan/tour)· composer vendor·model 外显(新建/会话内统一 `vendor · model 档位`);owner 直驱,opus/codex/kimi 三 agent 分工 + fable5 规划 review,基线 lib 1472 / web 0 失败(321–325 env 波动)/ vitest 392
- **v0.9.5** kimi-code 第五 vendor harness(`kimi acp` 长驻 stdio 薄壳,复用共享 ACP core:resume→load→new 阶梯 · skip auto-allow / hitl fail-closed · roleless-only · remote NotImplemented)· 五 vendor 全局 MCP 对称注册(+`~/.kimi-code/mcp.json`)· cost None 仿 opencode · 真机 smoke 全链路(spawn/dispatch/collect + live `/model`);dev = kimi-code 单会话一口气(owner 钦点),规划复核确认基线 1433/0
- **v0.9.3 增量**(未单独 bump)四 vendor 全局 MCP 对称注册(Claude/Codex/Grok/OpenCode 任意主会话可编排)+ `/mcp` 出 auth_layer 修委派父边 + spawn 响应 `caller` 字段
- **v0.9.2** 项目↔主机绑定(host 归 project、spawn 去 host 参、`project_init` op、卫星项目 import)· live 容量 50 LRU 挤停 · 团队拓扑树 · A2A 返回限幅防父会话膨胀 · 新建项目自由选主机
- **v0.10.5** 自进化闭环:研究 Google WikiSkill(arXiv 2608.27454,论文无代码)→「引方法不引依赖」。引擎侧 zero-LLM 测量底座(pump 确定性 `invoked_skills` 检测 + evolution 聚合 steered/per-vendor 分层/invoked 子集);用户空间自治闭环 `evolution-troika`(6 角色 + 4 机器门代替人,住 hub 不入 repo,守 R1-R4)。HITL 早退红线故全自治合法
- **v0.9.1** MCP 单前缀修复(去双前缀)+ 非 ccteam 主会话 admin fallback
- **v0.9.0** Agent2Agent 底座:`session_*` 5 工具 + per-session principal `(sid,secret)` + 委派语义(路由非引擎)+ 反向连接跨机 + 引擎零内置 persona(废除 cto)
- **v0.8.x 系列** 协议轴(stream-json 默认 / terminal 维护期)· 插件市场(track-upstream + plugin type)· 多租户软分区(档0/档1)· web chat-shell 统一重写 · IM 通用模式 → `git log` + `docs-local/versions/v0-8-*/`
